{
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "name": "lerna-monorepo",
  "scripts": {
    "start": "lerna run start",
    "test": "lerna run test",
    "clean": "npx lerna clean -y",
    "new-version": "lerna version --conventional-commits --yes",
    "diff": "lerna diff"
  }
}
